import React from 'react';
import './Board.css'


const Board = (props) => {

  return (
    <div className='board-container'>
      {props.location.quantProps.map((tube) => )}
    </div>
  );
}

export default Board;

